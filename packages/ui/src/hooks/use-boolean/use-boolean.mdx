import { Meta, Primary } from '@storybook/blocks';
import * as UseBooleanStories from './use-boolean.stories';

<Meta of={UseBooleanStories} />

# useBoolean

`useBoolean` is an utility hook to deal with boolean states. It avoid you to rewrite each time an handler to activate/deactivate/toggle the current state.

## Demo

<Primary />
