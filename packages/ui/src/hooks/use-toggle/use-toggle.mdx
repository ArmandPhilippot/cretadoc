import { Meta, Primary } from '@storybook/blocks';
import * as UseToggleStories from './use-toggle.stories';

<Meta of={UseToggleStories} />

# useToggle

`useToggle` is an utility hook to deal with boolean states that can only be toggled.

See also: [`useBoolean` hook](/docs/hooks-useboolean--docs).

## Demo

<Primary />

{/* cspell:ignore-word useboolean */}
