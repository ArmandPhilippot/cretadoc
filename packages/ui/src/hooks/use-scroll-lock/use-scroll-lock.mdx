import { Meta, Primary } from '@storybook/blocks';
import * as UseScrollLockStories from './use-scroll-lock.stories';

<Meta of={UseScrollLockStories} />

# useScrollLock

`useScrollLock` is a React hook that lets you disable the scroll on the given element or on the body.

## Demo

You should see a scrollbar when the checkbox is unchecked and the scrollbar disappear when it is checked.

<Primary />
