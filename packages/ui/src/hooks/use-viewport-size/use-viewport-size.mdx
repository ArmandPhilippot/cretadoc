import { Meta, Primary } from '@storybook/blocks';
import * as UseViewportSizeStories from './use-viewport-size.stories';

<Meta of={UseViewportSizeStories} />

# useViewportSize

`useViewportSize` is a React hook that lets you retrieve the current viewport size.

## Demo

<Primary />
