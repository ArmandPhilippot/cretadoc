import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as SkipToStories from './skip-to.stories';

<Meta of={SkipToStories} />

# Skip to

A `<SkipTo />` component allows screen reader and keyboard users to bypass navigation rather than tabbing through it. It is commonly used to let users quickly access the main content of the page.

<Primary />

## Props

<ArgTypes of={SkipToStories} />
