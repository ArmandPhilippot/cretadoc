import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as ToggleGroupStories from './toggle-group.stories';

<Meta of={ToggleGroupStories} />

# Toggle Group

A toggle group is a group of `<ToggleItem />` that can be used to quickly switch between states.

<Primary />

## Props

<ArgTypes of={ToggleGroupStories} />
