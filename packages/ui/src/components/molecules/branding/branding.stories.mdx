import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Img } from '../../atoms';
import { Branding } from './branding';

<Meta
  title="Components/Molecules/Branding"
  component={Branding}
  args={{
    brand: 'A name',
    to: '#',
  }}
  argTypes={{
    brand: {
      control: {
        type: 'text',
      },
      description: 'Define your branding name.',
      type: {
        name: 'string',
        required: true,
      },
    },
    logo: {
      control: {
        type: null,
      },
      description: 'Define an `Img` component to use a logo.',
      table: {
        category: 'Options',
      },
      type: {
        name: 'string',
        required: false,
      },
    },
    to: {
      control: {
        type: 'text',
      },
      description: 'Define the link target (usually the homepage).',
      type: {
        name: 'string',
        required: true,
      },
    },
  }}
/>

export const Template = (args) => <Branding {...args} />;

# Branding

A branding component is commonly use in the website heading. It provides information about the visited website.

## Contents

A branding always has a brand. In addition, you can provide a logo.

<Canvas isColumn>
  <Story name="Contents: brand only">{Template.bind({})}</Story>
  <Story
    name="Contents: brand and logo"
    args={{
      logo: <Img src="https://picsum.photos/640/480" alt="Branding logo" />,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Playground

<Canvas>
  <Story
    name="Playground"
    args={{
      logo: <Img src="https://picsum.photos/200/300" alt="Branding logo" />,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
