import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as TruncateStories from './truncate.stories';

<Meta of={TruncateStories} />

# Truncate

A `<Truncate />` component is used to limit the size of a text. It can be useful in context of navigation when the items should not exceed a certain width for example.

**Note:** The full text is readable by hovering the truncated text.

<Primary />

## Props

<ArgTypes of={TruncateStories} />
