import { ArgTypes, Canvas, Meta, Primary } from '@storybook/blocks';
import * as ListItemStories from './list-item.stories';

<Meta of={ListItemStories} />

# List Item

A list item can be used inside an ordered list or unordered list to display some data.

<Primary />

## Borders

A list item can be bordered or not.

### Without borders

<Canvas of={ListItemStories.Default} />

### With borders

<Canvas of={ListItemStories.Border} />

### Borders position

You can also define the desired borders.

#### Left

<Canvas of={ListItemStories.BorderLeft} />

#### Right

<Canvas of={ListItemStories.BorderRight} />

#### Bottom

<Canvas of={ListItemStories.BorderBottom} />

#### Top

<Canvas of={ListItemStories.BorderTop} />

#### Block

<Canvas of={ListItemStories.BorderBlock} />

#### Inline

<Canvas of={ListItemStories.BorderInline} />

### Borders color

The component also allows you to define the color of the border.

#### Critical

<Canvas of={ListItemStories.BorderCritical} />

#### Info

<Canvas of={ListItemStories.BorderInfo} />

#### Inverted

<Canvas of={ListItemStories.BorderInverted} />

#### Muted

<Canvas of={ListItemStories.BorderMuted} />

#### Primary

<Canvas of={ListItemStories.BorderPrimary} />

#### Regular

<Canvas of={ListItemStories.BorderRegular} />

#### Success

<Canvas of={ListItemStories.BorderSuccess} />

#### Warning

<Canvas of={ListItemStories.BorderWarning} />

### Borders size

You can also define the size of the borders.

#### Small (sm)

<Canvas of={ListItemStories.BorderSM} />

#### Medium (md)

<Canvas of={ListItemStories.BorderMD} />

#### Large (lg)

<Canvas of={ListItemStories.BorderLG} />

## Marker

A list item can have a marker or not.

### With marker

<Canvas of={ListItemStories.WithMarker} />

### Without marker

<Canvas of={ListItemStories.WithoutMarker} />

### Custom marker

You can also use a custom marker like a [CSS keyword](https://developer.mozilla.org/en-US/docs/Web/CSS/list-style-type) or an unicode emoji.

#### CSS keyword

<Canvas of={ListItemStories.CSSKeywordMarker} />

#### Unicode

<Canvas of={ListItemStories.UnicodeMarker} />

## Props

<ArgTypes of={ListItemStories} />
