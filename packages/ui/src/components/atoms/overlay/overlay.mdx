import { ArgTypes, Canvas, Meta, Primary } from '@storybook/blocks';
import * as OverlayStories from './overlay.stories';

<Meta of={OverlayStories} />

# Overlay

An `<Overlay />` component is used to separate foreground from the background content and to overlay the whole screen.

<Primary />

## Accessibility

When using an overlay, you should disable the body overflow to prevent the background content to be scrolled while the overlay is active.

## Props

<ArgTypes of={OverlayStories} />
