import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';
import { Checkbox } from './checkbox';

<Meta
  title="Components/Atoms/Forms/Checkbox"
  component={Checkbox}
  args={{ isChecked: false, isDisabled: false, isRequired: false }}
  argTypes={{
    id: {
      control: {
        type: null,
      },
      description: 'Set an id to the input element.',
      table: {
        category: 'Attributes',
      },
      type: {
        name: 'string',
        required: true,
      },
    },
    isChecked: {
      control: {
        type: 'boolean',
      },
      description: 'Define the field state: either checked on unchecked.',
      table: {
        category: 'Options',
        defaultValue: { summary: false },
      },
      type: {
        name: 'boolean',
        required: false,
      },
    },
    isDisabled: {
      control: {
        type: 'boolean',
      },
      description: 'Should the field be disabled?',
      table: {
        category: 'Options',
        defaultValue: { summary: false },
      },
      type: {
        name: 'boolean',
        required: false,
      },
    },
    isRequired: {
      control: {
        type: 'boolean',
      },
      description: 'Should the field be required?',
      table: {
        category: 'Options',
        defaultValue: { summary: false },
      },
      type: {
        name: 'boolean',
        required: false,
      },
    },
    name: {
      control: {
        type: null,
      },
      description: 'A field name.',
      table: {
        category: 'Attributes',
      },
      type: {
        name: 'string',
        required: true,
      },
    },
  }}
/>

export const Template = ({ isChecked: checked, onChange, ...args }) => {
  const [isChecked, setIsChecked] = useState(checked);
  const handleChange = (e) => {
    setIsChecked(e.target.checked);
  };
  return <Checkbox {...args} isChecked={isChecked} onChange={handleChange} />;
};

# Checkbox

A `Checkbox` component allows you to turn single values on and off in a form.

## States

A checkbox can be:

- unchecked
- checked
- disabled
- required

<Canvas>
  <Story name="State: unchecked">
    <Checkbox id="unchecked" name="unchecked" onChange={() => {}} />
  </Story>
  <Story name="State: checked">
    <Checkbox id="checked" isChecked name="checked" onChange={() => {}} />
  </Story>
  <Story name="State: disabled">
    <Checkbox id="disabled" isDisabled name="disabled" onChange={() => {}} />
  </Story>
  <Story name="State: required">
    <Checkbox id="required" isRequired name="required" onChange={() => {}} />
  </Story>
</Canvas>

## Playground

<Canvas>
  <Story
    name="Playground"
    args={{
      id: 'playground-field',
      name: 'playground-field',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
