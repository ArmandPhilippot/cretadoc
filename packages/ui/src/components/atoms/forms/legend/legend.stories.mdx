import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Fieldset } from '../fieldset';
import { Legend } from './legend';

<Meta
  title="Components/Atoms/Forms/Legend"
  component={Legend}
  args={{
    children: 'The fieldset legend',
    color: 'regular',
    size: 'md',
  }}
  argTypes={{
    color: {
      control: {
        type: 'select',
      },
      options: [
        'critical',
        'info',
        'inverted',
        'muted',
        'primary',
        'regular',
        'success',
        'warning',
      ],
      description: 'Set the color of the legend.',
      table: {
        category: 'Options',
        defaultValue: { summary: 'regular' },
      },
      type: {
        name: 'string',
        required: false,
      },
    },
    size: {
      control: {
        type: 'select',
      },
      options: ['xs', 'sm', 'md', 'lg'],
      description: 'Set the size of the legend.',
      table: {
        category: 'Options',
        defaultValue: { summary: 'md' },
      },
      type: {
        name: 'string',
        required: false,
      },
    },
  }}
/>

export const Template = (args) => (
  <Fieldset>
    <Legend {...args} />
  </Fieldset>
);

# Legend

A legend represents a caption for the controls inside a `<Fieldset />` component.

## Color

The legend can have a different color than the foreground:

<Canvas>
  <Story name="Color: critical" args={{ color: 'critical' }}>
    {Template.bind({})}
  </Story>
  <Story name="Color: info" args={{ color: 'info' }}>
    {Template.bind({})}
  </Story>
  <Story name="Color: inverted" args={{ color: 'inverted' }}>
    {Template.bind({})}
  </Story>
  <Story name="Color: muted" args={{ color: 'muted' }}>
    {Template.bind({})}
  </Story>
  <Story name="Color: primary" args={{ color: 'primary' }}>
    {Template.bind({})}
  </Story>
  <Story name="Color: regular" args={{ color: 'regular' }}>
    {Template.bind({})}
  </Story>
  <Story name="Color: success" args={{ color: 'success' }}>
    {Template.bind({})}
  </Story>
  <Story name="Color: warning" args={{ color: 'warning' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Size

You can also control the font-size used for the legend:

<Canvas>
  <Story name="Size: extra small" args={{ size: 'xs' }}>
    {Template.bind({})}
  </Story>
  <Story name="Size: small" args={{ size: 'sm' }}>
    {Template.bind({})}
  </Story>
  <Story name="Size: medium" args={{ size: 'md' }}>
    {Template.bind({})}
  </Story>
  <Story name="Size: large" args={{ size: 'lg' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Playground

<Canvas>
  <Story name="Playground" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
