import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Link } from './link';

<Meta
  title="Components/Atoms/Link"
  component={Link}
  args={{}}
  argTypes={{
    children: {
      control: {
        type: 'text',
      },
      description: 'The link body.',
      type: {
        name: 'string',
        required: true,
      },
    },
    hrefLang: {
      control: {
        type: 'text',
      },
      description: 'Hints at the human language of the linked URL.',
      table: {
        category: 'Options',
      },
      type: {
        name: 'string',
        required: false,
      },
    },
    to: {
      control: {
        type: 'text',
      },
      description: 'The link target.',
      type: {
        name: 'string',
        required: true,
      },
    },
  }}
/>

export const Template = (args) => <Link {...args} />;

# Link

A link is used to create a hyperlink to another location. It can be a web page, a file, an email address or even another location in the same page.

## Language

You can use the `hreflang` attribute to inform the user about the lang used in the link target.

<Canvas>
  <Story name="Language">
    <p style={{ lineHeight: 1.5 }}>
      {'You can find Cretadoc '}
      <Link to="https://github.com/ArmandPhilippot/cretadoc" hrefLang="en">
        {'on Github'}
      </Link>
      {
        '. Nobis iste enim. Necessitatibus hic harum. Quo expedita non. Culpa ipsum natus et quo et qui ut et rerum. Vel dolores ipsam odit. Aperiam labore ea qui rerum voluptatem minima.'
      }
    </p>
  </Story>
</Canvas>

## Playground

<Canvas>
  <Story
    name="Playground"
    args={{
      children: 'Your link',
      to: '#',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
