import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as FooterStories from './footer.stories';

<Meta of={FooterStories} />

# Footer

A `<Footer />` component is used to represent a footer for its nearest ancestor.

<Primary />

## Props

<ArgTypes of={FooterStories} />
