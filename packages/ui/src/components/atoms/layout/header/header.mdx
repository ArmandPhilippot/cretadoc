import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as HeaderStories from './header.stories';

<Meta of={HeaderStories} />

# Header

A `<Header />` component is used to represent introductory content.

<Primary />

## Props

<ArgTypes of={HeaderStories} />
