import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as ArticleStories from './article.stories';

<Meta of={ArticleStories} />

# Article

An `<Article />` component is used to represent a self-contained composition which is intended to be independently distributable or reusable. So every article should have at least a `<Heading />`.

<Primary />

## Props

<ArgTypes of={ArticleStories} />
