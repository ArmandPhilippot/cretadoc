import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as NavStories from './nav.stories';

<Meta of={NavStories} />

# Nav

A `<Nav />` component is used to wrap navigation elements.

<Primary />

## Props

<ArgTypes of={NavStories} />
