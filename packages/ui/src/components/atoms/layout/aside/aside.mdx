import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as AsideStories from './aside.stories';

<Meta of={AsideStories} />

# Aside

An `<Aside />` component is used to represent a portion of a document whose content is indirectly related to the main content. It is commonly used as sidebar.

<Primary />

## Props

<ArgTypes of={AsideStories} />
