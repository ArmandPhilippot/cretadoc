import { ArgTypes, Meta, Primary } from '@storybook/blocks';
import * as MainStories from './main.stories';

<Meta of={MainStories} />

# Main

A `<Main />` component is used to represent the dominant content of the document body.

<Primary />

## Props

<ArgTypes of={MainStories} />
